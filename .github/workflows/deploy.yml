name: android_app
description: A Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 使っているなら残す（localizationsを使う場合）
  flutter_localizations:
    sdk: flutter

  # ✅ intl は書かない（flutter_localizations が intl 0.19.0 に固定するため）
  # intl: ^0.20.2  ← これがあると衝突するので削除

  cupertino_icons: ^1.0.8

  # ↓↓↓ あなたの既存依存はここにそのまま残す ↓↓↓
  # example_package: ^1.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/
